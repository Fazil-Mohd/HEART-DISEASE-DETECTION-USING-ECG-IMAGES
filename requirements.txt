# ──────────────────────────────────────────────────────────────
#  ECG Heart Disease Detection — Requirements
#  Install with:  pip install -r requirements.txt
# ──────────────────────────────────────────────────────────────

# ── Deep Learning ──────────────────────────────────────────────
tensorflow>=2.15.0,<2.20.0   # 2.20.0 not on Linux PyPI — resolves to 2.18.x on Render
keras>=3.0.0                 # let pip pick compatible keras for the resolved tensorflow
h5py==3.15.1

# ── ML / Data Science ─────────────────────────────────────────
scikit-learn==1.7.2          # compute_class_weight in ml_model.train_model()
numpy==2.2.6                 # array ops in ml_model.py, utils.py, tasks.py
pandas==2.3.3
scipy==1.15.3

# ── Explainability (LIME) ──────────────────────────────────────
lime==0.2.0.1                # LimeImageExplainer in ml_model.generate_lime_explanation()
scikit-image==0.25.2         # skimage.segmentation.mark_boundaries used by LIME

# ── Image Processing & Visualisation ──────────────────────────
Pillow==12.1.1               # PIL.Image in utils.py and tasks.py
matplotlib==3.10.8           # result image overlay + LIME plot (Agg backend)
opencv-python==4.13.0.92     # Image processing utilities

# ── Web Framework ──────────────────────────────────────────────
Django==5.2.11
asgiref==3.11.1
sqlparse==0.5.5

# ── Task Queue ─────────────────────────────────────────────────
celery==5.6.2
redis==7.3.0                 # Python Redis client used as broker & result backend
kombu==5.6.2

# ── PDF Generation ─────────────────────────────────────────────
xhtml2pdf==0.2.17            # pisa.CreatePDF in utils.generate_pdf_report_content()
reportlab==4.4.10

# ── Production / Deployment ────────────────────────────────────────────────────
gunicorn==23.0.0             # production WSGI server (replaces manage.py runserver)
whitenoise==6.9.0            # serves static files without a separate web server
dj-database-url==2.3.0       # parse DATABASE_URL env var → DATABASES dict
psycopg2-binary==2.9.10      # PostgreSQL adapter for Django
huggingface_hub==0.32.1      # download model from Hugging Face Hub at startup
